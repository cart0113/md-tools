---
name: reformat-tables
description: Reformat markdown tables in a folder to fixed-width alignment
---

Reformat all markdown tables in a target folder so columns are padded and
aligned for fixed-width fonts, with lines kept under 120 characters.

Run:

```bash
python-main examples/reformat_tables.py <target_folder>
```

Report which files were reformatted.
